# Installing deps (tested for debian 12)
```bash
apt install -y curl sudo
```

# To install docker / docker compose
```bash
curl -fsSL https://raw.githubusercontent.com/GustavPetterssonBjorklund/scripts/main/legacy/docker/install.sh | sudo bash
```
