# scripts

## Interactive installer
```bash
curl -fsSL https://raw.githubusercontent.com/GustavPetterssonBjorklund/scripts/main/install-menu.sh | bash
```
Use the spacebar checklist to pick which tool installers to run (e.g., docker, ovpntmp).
